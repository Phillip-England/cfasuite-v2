[project]
name = "cfasuite-e2e"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "playwright>=1.51.0",
  "pytest>=8.3.5",
]

[tool.pytest.ini_options]
testpaths = ["tests_py"]
addopts = "-q"
